import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    headerContainer: { flexDirection: "row" },
    screenContainer: { justifyContent: "center", alignItems: "center", height: "100%" },
    title: { fontSize: 24 },
    bold: { fontWeight: "bold" },
})